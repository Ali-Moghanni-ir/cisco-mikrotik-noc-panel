---
- name: Cisco IOS Extended ACL Management
  hosts: all
  gather_facts: no

  tasks:
    - name: Ensure ACL exists and apply rule
      cisco.ios.ios_config:
        lines:
          - "{{ action }} {{ protocol }} host {{ src_ip }} host {{ dst_ip }} eq {{ port }}"
        parents: "ip access-list extended {{ acl_name }}"